# 需求质量检查清单 - 航运船期查询功能

**检查类型**: Requirements Quality Checklist  
**检查深度**: Standard  
**检查对象**: Author Review  
**创建时间**: 2026年1月9日  
**功能分支**: `002-shipping-schedule`

---

## 检查说明

本清单基于"需求即单元测试"的理念,确保每一条需求都清晰、可测试、独立且完整。请逐项检查以下条目,确保需求规格说明符合质量标准。

---

## ✅ 基础完整性检查

### 1. 文档结构完整性
- [ ] **假设章节**存在且包含关键假设(数据来源、触发阈值、查询条件规则)
- [ ] **澄清章节**记录所有澄清会议的决策(共9个已澄清问题)
- [ ] **范围外声明**明确列出不包含的功能(管理、详情页、订阅)
- [ ] **用户故事章节**包含至少3个以上的核心场景

### 2. 术语一致性
- [ ] 全文统一使用"船期"而非"航班"
- [ ] ETD(预计发运时间)术语在所有场景中保持一致
- [ ] 港口代码格式统一(如 XMN, SGN, HKG)
- [ ] 时间单位统一使用"天"而非"days"或"日"

### 3. 数据范围明确性
- [ ] 静态数据量明确(至少30条全球主流船期)
- [ ] 时间跨度明确(过去1个月 + 未来3个月)
- [ ] 港口数据合并规则已说明(需与现有 `ports.json` 合并)

---

## ✅ 用户故事质量检查

### Story 1 - 基础船期查询

#### 独立性 (I in INVEST)
- [ ] 用户故事可独立实现和测试,不依赖其他故事的完成
- [ ] 独立测试描述清晰:"进入页面 → 验证数据加载和展示"

#### 可测试性 (Testable)
- [ ] **AS1.1**: Given-When-Then 结构完整,验证点明确(显示列表 + 包含字段)
- [ ] **AS1.2**: 验证条件可观察(信息完整性 + 格式清晰)
- [ ] **AS1.3**: 分页条件明确(30条以上记录 → 支持分页)
- [ ] 每个 Acceptance Scenario 都可转换为自动化测试用例

#### 优先级合理性
- [ ] P1 优先级合理(基础展示是其他功能的前提)
- [ ] 优先级理由清晰:"核心基础,用户必须能够看到船期数据"

---

### Story 2 - 按起运港/目的港查询

#### 独立性
- [ ] 用户故事虽依赖 Story 1 的数据展示,但查询逻辑可独立实现
- [ ] 独立测试描述清晰:"选择港口 → 返回匹配列表"

#### 可测试性
- [ ] **AS2.1**: 单一条件查询(起运港)可验证
- [ ] **AS2.2**: 单一条件查询(目的港)可验证
- [ ] **AS2.3**: 组合条件查询(起运港 + 目的港)可验证
- [ ] **AS2.4**: 空结果处理(无匹配 → 提示信息)可验证
- [ ] 测试数据需求明确(需包含"上海"→"鹿特丹"航线)

#### 边界条件覆盖
- [ ] 覆盖单一条件场景(起运港 OR 目的港)
- [ ] 覆盖组合条件场景(起运港 AND 目的港)
- [ ] 覆盖空结果场景(无匹配数据)

---

### Story 3 - 港口自动补全

#### 独立性
- [ ] 自动补全功能可独立于查询逻辑实现
- [ ] 独立测试描述清晰:"输入字符 → 显示匹配选项"

#### 可测试性
- [ ] **AS3.1**: 英文输入触发("sha" → 显示 Shanghai)可验证
- [ ] **AS3.2**: 中文输入触发("上海" → 显示上海)可验证
- [ ] **AS3.3**: 选择行为(点击 → 填入输入框)可验证
- [ ] **AS3.4**: 触发阈值验证(< 2字符 → 不触发)可验证
- [ ] **AS3.5**: 空结果处理(无匹配 → 提示信息)可验证

#### 性能约束
- [ ] 防抖时间明确(300毫秒)
- [ ] 触发阈值明确(2个字符以上)
- [ ] 匹配范围明确(所有已保存的港口)

---

### Story 4 - 按ETD时间范围查询

#### 独立性
- [ ] 时间筛选功能可独立于港口查询实现
- [ ] 独立测试描述清晰:"设置时间范围 → 返回匹配船期"

#### 可测试性
- [ ] **AS4.1**: 起始时间筛选可验证
- [ ] **AS4.2**: 结束时间筛选可验证
- [ ] **AS4.3**: 时间范围筛选(起始 + 结束)可验证
- [ ] **AS4.4**: 组合条件筛选(时间 + 港口)可验证

#### 数据类型约束
- [ ] 时间输入方式明确(使用 `input[type="date"]`)
- [ ] 时间格式一致性(YYYY-MM-DD)
- [ ] 时间比较逻辑明确(>= 起始时间 AND <= 结束时间)

---

## ✅ 交互行为一致性检查

### 查询触发方式
- [ ] 澄清记录明确:手动查询(点击按钮)
- [ ] 所有用户故事的 Acceptance Scenarios 与手动触发方式一致
- [ ] 无自动触发查询的场景描述(避免性能问题)

### 自动补全行为
- [ ] 触发阈值统一(2个字符以上)
- [ ] 防抖时间统一(300毫秒)
- [ ] 匹配范围统一(所有港口)
- [ ] 选择行为统一(点击填入)

### 分页行为
- [ ] 每页数量明确(10条)
- [ ] 分页触发条件明确(总数 > 10条)
- [ ] 分页交互方式需补充说明(页码 or 上一页/下一页)

---

## ✅ 数据模型与UI一致性检查

### 必需字段验证
- [ ] 起运港(Origin Port) - 用户故事中明确需要
- [ ] 目的港(Destination Port) - 用户故事中明确需要
- [ ] ETD(预计发运时间) - 用户故事中明确需要
- [ ] 运输耗时(Transit Days) - 用户故事中明确需要
- [ ] 承运公司(Carrier) - 用户故事中明确需要

### 显示格式验证
- [ ] 运输耗时单位:"X天"(澄清记录已明确)
- [ ] ETD格式需在 `data-model.md` 中明确(YYYY-MM-DD)
- [ ] 港口展示格式需明确(中文名 or 英文名 or 港口代码)

---

## ✅ 技术架构一致性检查

### 路由与导航
- [ ] 澄清记录明确:顶部导航栏新增"船期查询"菜单项
- [ ] `plan.md` 中确认不使用 `vue-router`(使用 `component :is`)
- [ ] 需确认现有导航栏实现方式(位于 `App.vue` or 独立组件)

### 数据源
- [ ] 澄清记录明确:静态JSON数据(`schedules.json`)
- [ ] 需确认 `schedules.json` 的存放位置(`src/assets/data/`)
- [ ] 需确认 `ports.json` 合并策略(新增船期用到的港口代码)

### UI组件复用
- [ ] 自动补全逻辑可参考现有 `PortSearch.vue`
- [ ] 分页逻辑可复用现有 `Pagination.vue`
- [ ] 列表展示逻辑可参考现有 `PortList.vue`

---

## ✅ 缺失场景识别

### 排序行为
- [ ] 澄清记录明确:默认按 ETD 升序排列
- [ ] ⚠️ **缺失场景**: 用户是否可手动切换排序方式(ETD降序、运输耗时等)?
- [ ] 建议:如无需手动排序,需在"范围外声明"中明确

### 错误处理
- [ ] ✅ 已覆盖:无匹配结果提示(Story 2, AS2.4)
- [ ] ✅ 已覆盖:无匹配港口提示(Story 3, AS3.5)
- [ ] ⚠️ **缺失场景**: 数据加载失败时的错误提示
- [ ] 建议:补充 Story 1 的错误场景(如 `schedules.json` 加载失败)

### 查询重置
- [ ] ⚠️ **缺失场景**: 用户如何清空查询条件并重置为初始状态?
- [ ] 建议:补充"重置查询"用户故事或在 Story 2 中增加 AS

### 空数据状态
- [ ] ⚠️ **缺失场景**: 初始加载时若 `schedules.json` 为空数组的处理
- [ ] 建议:补充空数据状态的 UI 提示(如"暂无船期数据")

---

## ✅ 可追溯性检查

### 澄清决策追溯
- [ ] 每个关键决策都在"Clarifications"章节中有记录
- [ ] 每个 User Story 的优先级理由可追溯到业务需求
- [ ] 技术选型(如无 `vue-router`)可追溯到 `plan.md` 和 `research.md`

### 测试数据追溯
- [ ] Story 2 需要的测试数据:"上海" → "鹿特丹"航线
- [ ] Story 3 需要的测试数据:包含"Shanghai"和"上海"的港口
- [ ] Story 4 需要的测试数据:不同 ETD 的船期记录
- [ ] 需在 `data-model.md` 或实现阶段确认测试数据集

---

## 📋 改进建议清单

### 高优先级改进
1. **补充错误处理场景**: 在 Story 1 中增加"数据加载失败"的 Acceptance Scenario
2. **明确排序范围**: 在"范围外声明"中补充"不支持手动切换排序方式"(若不需要)
3. **补充重置功能**: 增加"清空查询条件"的用户故事或场景
4. **明确港口显示格式**: 在 AS 中补充港口显示规则(如"上海 (SHA)"或"Shanghai")

### 中优先级改进
5. **补充分页交互细节**: 明确分页组件的交互方式(页码点击 or 上下页按钮)
6. **补充空数据状态**: 增加"无船期数据"的 UI 提示场景
7. **补充时间输入验证**: 在 Story 4 中增加"起始时间晚于结束时间"的错误处理

### 低优先级建议
8. **增加性能验证**: 在 Story 3 中补充"300ms 防抖生效"的验证场景
9. **增加可访问性验证**: 补充键盘导航支持(如方向键选择自动补全选项)

---

## ✅ 检查通过标准

- [ ] 所有"✅"章节的子项全部勾选通过
- [ ] "📋 改进建议清单"中的高优先级改进已完成或已确认不需要
- [ ] 所有"⚠️ 缺失场景"已处理或明确归入"范围外声明"
- [ ] 至少一位团队成员完成 Peer Review

---

## 签名确认

- **需求作者**: ___________  日期: ___________
- **技术评审**: ___________  日期: ___________
- **产品负责人**: ___________  日期: ___________

---

**备注**: 本清单基于 Speckit 方法论的"需求即测试"原则生成,确保每条需求都可转换为可执行的测试用例。
